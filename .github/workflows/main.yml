name: Python Bot

on: workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Start Bot
      run: curl -sO https://github.com/luanwillianzh/BotMb-Telegram/archive/refs/heads/main.zip ; unzip main.zip ; python3 main.py
